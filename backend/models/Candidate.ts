  
  // models/Candidate.ts
  export interface Candidate {
    id: string;
    name: string;
    partyName: string;
    slogan: string;
    // Add other fields as needed
  }
  
  // Add more models as necessary